<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HRMApp.View.TrainingListPage"
             xmlns:viewmodel="clr-namespace:HRMApp.ViewModel"
             Title="TrainingListPage">
    <CollectionView ItemsSource="{Binding Courses}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Margin="10" Padding="10">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:TrainingListViewModel}}, Path=GoToDetailCommand}" CommandParameter="{Binding .}" />
                    </Frame.GestureRecognizers>
                    <StackLayout>
                        <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                        <Label Text="{Binding QuestionCount, StringFormat='Số câu hỏi: {0}'}" TextColor="Gray"/>
                        <Label Text="{Binding StatusDisplay}" TextColor="{Binding StatusColor}" FontAttributes="Italic"/>
                    </StackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>